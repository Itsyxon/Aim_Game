import React, { useState } from 'react';
import styles from '../styles/SingleDot.module.css';

const SingleDot = () => {

    return (
        <div className={`${styles.singleDot}`} >

        </div>
    );
};

export default SingleDot;